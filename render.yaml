services:
  - name: portfolio-web
    type: web
    runtime: docker
    dockerfilePath: Dockerfile
    buildFilter: "*"
    envVars:
      NODE_ENV: production
    autoDeploy: true
    plan: free # or 'starter+' for more resources
    healthCheckPath: /
    port: 3000