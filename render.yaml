```yaml
services:
  - type: web
    name: portfolioweb
    env: node
    plan: starter
    build:
      env: production
    routes:
      - path: /
    healthCheck:
      path: /
      interval: 30000
```